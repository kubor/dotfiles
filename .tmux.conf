# prefixキーをC-tに変更する
set -g prefix C-t
# 256色使用する
set -g default-terminal "screen-256color"
# 文字コードをutf-8にする
set -g status-utf8 on
# デフォルトシェルをZshにする
set -g default-shell /bin/zsh
# | でペインを縦に分割する
bind | split-window -h
# - でペインを横に分割する
bind - split-window -v
